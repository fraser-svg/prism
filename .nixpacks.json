{
  "providers": ["node"],
  "phases": {
    "install": {
      "cmds": ["npm install"]
    },
    "build": {
      "cmds": ["npm run build:web"]
    }
  },
  "start": "npm rebuild better-sqlite3 && node apps/web/dist/server/index.js"
}
