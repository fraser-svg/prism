{
  "name": "prism",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck:core": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/memory/tsconfig.json --noEmit && tsc -p packages/orchestrator/tsconfig.json --noEmit && tsc -p packages/guardian/tsconfig.json --noEmit && tsc -p packages/execution/tsconfig.json --noEmit && tsc -p packages/workspace/tsconfig.json --noEmit",
    "verify": "npm run typecheck:core && npm run test",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
